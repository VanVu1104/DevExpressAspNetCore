@model IEnumerable<DevExtremeAspNetCore.ViewModels.NPLViewModel>
@using Newtonsoft.Json

@{
    ViewBag.Title = "Danh sách NPL";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/css/npl.css" rel="stylesheet" />

<div class="container">
    <div id="gridContainer" style="margin-top:20px;"></div>
</div>

<!-- Popup thêm/sửa -->
<div id="popupNPL" style="display:none;">
    <form>
        <div class="mb-2">
            <label>Tên nguyên phụ liệu</label>
            <input type="text" name="TenNpl" class="form-control" />
        </div>
        <div class="mb-2">
            <label>Màu</label>
            <input type="text" name="ColorNpl" class="form-control" />
        </div>
        <div class="mb-2">
            <label>Loại</label>
            <input type="text" name="Loai" class="form-control" />
        </div>
        <div class="mb-2">
            <label>Đơn vị</label>
            <input type="text" name="DonVi" class="form-control" />
        </div>
        <div class="mb-2">
            <label>Số lượng</label>
            <input type="number" name="SoLuong" class="form-control" min="0" required />
        </div>
        <div class="mb-2">
            <label>Vật tư</label>
            <input type="text" name="VatTu" class="form-control" />
        </div>
        <div>
            <label>Hình ảnh</label>
            <div id="popupUploader"></div>

            <!-- input ẩn -->
            <input type="file" id="cameraInput" style="display:none;" accept="image/*" capture="environment" />
            <input type="file" id="fileInput" style="display:none;" accept="image/*" multiple />

            <div class="mb-2">
                <div style="margin-bottom:10px; display:flex; gap:10px;">
                    <button type="button" id="btnOpenCamera" class="btn btn-secondary">📷 Mở Camera</button>
                    <button type="button" id="btnUploadImage" class="btn btn-info">📂 Upload ảnh</button>
                    <button type="button" id="btnCapture" class="btn btn-success" style="display:none;">Chụp ảnh</button>
                    <button type="button" id="btnCloseCamera" class="btn btn-danger" style="display:none;">Đóng camera</button>
                </div>

                <video id="videoCam" autoplay playsinline style="width:100%; max-height:250px; display:none; border:1px solid #ccc; border-radius:6px;"></video>
                <canvas id="canvasPhoto" style="display:none;"></canvas>
                <div id="imagePreview" style="margin-top:10px; display:flex; gap:10px; flex-wrap:wrap;"></div>
            </div>
       </div>

    </form>
</div>
<div id="popupUploadImage"></div>

@* PopUp Xem ảnh *@
<div id="popupImageViewer" style="display:none;">
    <div id="imageList" style="display:flex; flex-wrap:wrap; gap:10px;"></div>
</div>
<!-- Popup xem file --> 
<div id="popupFileViewer" style="display:none;"> 
    <ul id="fileList" style="list-style:none; padding:0;"></ul> 
</div>
@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
    <script src="~/js/npl.js"></script>
    <script>
        $(function(){
            var data = @Html.Raw(JsonConvert.SerializeObject(Model));
            var token = $('input[name="__RequestVerificationToken"]').val();
            var uploadUrl = '@Url.Action("UploadImageTemp", "NPL")';
            NPL.init(data, token, uploadUrl);
        });
    </script>
    <script>
        // Mở camera
        document.getElementById("openCameraBtn").addEventListener("click", function () {
            document.getElementById("cameraInput").click();
        });

        // Preview ảnh từ camera
        document.getElementById("cameraInput").addEventListener("change", function (event) {
            previewImages(event.target.files);
        });

        // Preview ảnh từ upload
        document.getElementById("uploadInput").addEventListener("change", function (event) {
            previewImages(event.target.files);
        });

        function previewImages(files) {
            const preview = document.getElementById("imagePreview");
            preview.innerHTML = "";
            for (let file of files) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    const img = document.createElement("img");
                    img.src = e.target.result;
                    preview.appendChild(img);
                };
                reader.readAsDataURL(file);
            }
        }
    </script>
    <script>$("#Layer_1").click();</script>
}
